run:
  timeout: 5m

linters:
  disable-all: true
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - gofmt
    - goimports
    - ineffassign
    - revive

issues:
  exclude-dirs:
    - tmp
